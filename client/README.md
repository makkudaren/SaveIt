SaveIt Technologies

Steps to install
1. Clone the repository
2. open terminal and enter the following:
    cd client
    npm install
3. create a .env file besides env.template. and enter the api key
4. npm run dev -> to start the local server.